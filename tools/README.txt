可将 VNTextPatch / rldev 等工具放在游戏的 _tools 目录，
或在软件界面「外部工具目录」中指定（例如 ComingHumming\_tools）。

RealLive：GalAutoTL 会在缺少 export_utf8 时自动下载 RLDev（kprl）并解包 SEEN.TXT，
一般不必再手动准备。工具缓存在 %APPDATA%\GalAutoTL\rldev-1.40-win32。

本程序不会默认安装 VNTextProxy DLL。
